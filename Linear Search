#include <stdio.h>

int main() {
    int arr[] = {10, 20, 30, 40, 50}, key = 30, i;
    for (i = 0; i < 5; i++) {
        if (arr[i] == key) {
            printf("Found at index %d\n", i);
            break;
        }
    }
    return 0;
}
